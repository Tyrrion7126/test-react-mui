import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#ff4829"
        },
        secondary: {
            main: "#5cbd95"
        },
        background: {
            dark: "#161616"
        }
    }
});

export default theme;